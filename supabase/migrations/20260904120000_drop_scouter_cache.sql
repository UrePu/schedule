-- ═══════════════════════════════════════════════════════════════════════════════
-- M_Schedule · `scouter_stat_cache` **드롭** — `!환산` 스크래핑 철회
-- ═══════════════════════════════════════════════════════════════════════════════
--
-- 하루 전 마이그레이션(`20260903180000_scouter_cache.sql`)이 만든 표를 되돌린다.
-- **그 파일은 지우지 않는다.** 마이그레이션은 역사이고 이미 원격에 적용됐다 — 되돌림은
-- 지우는 것이 아니라 **새 파일로 표현**한다.
--
-- ───────────────────────────────────────────────────────────────────────────────
-- 왜 드롭하는가 — 동작이 아니라 **약관**이다
-- ───────────────────────────────────────────────────────────────────────────────
-- 이 표는 maplescouter.com 페이지를 **헤드리스 브라우저로 열어** 읽어 온 값을 담고 있었다.
-- 발주자가 그쪽 이용약관(2026-07-11 시행)을 확인한 결과, 우리 구현이 여섯 조항에 걸린다:
--
--   · 제15조 1호 — 자동화된 수단(봇·스크립트·크롤러·스크래퍼·**헤드리스 브라우저**·MCP 등)
--                  으로 서비스에 접근하는 행위. 우리가 쓴 수단이 조문에 그대로 적혀 있다.
--   · 제15조 2호 — 서비스의 계산·환산 기능을 **타 서비스·제품·도구의 백엔드 또는 데이터
--                  소스로 사용**하는 행위. 카톡 봇 답장에 스탯을 실은 것이 정확히 이것이다.
--   · 제15조 4호 — 인증 등 접근제어·기술적 보호조치 우회.
--   · 제15조 3호·7호 — 엔드포인트 무단 추출 · 리버스 엔지니어링.
--   · 제14조 2항 — 운영자 동의 없이 **가공 데이터를 복제·전송**. ← 이 표가 곧 그 복제본이다.
--
-- 제17조는 이런 경우 **사전 통지 없이 차단**할 수 있다고 정한다.
-- 발주자 결정(2026-09-04): *"ㅇㅇ 카드로 변경해라."*
--
-- ★ **받아둔 복제 데이터를 남길 이유가 없다.** 코드에서 읽는 곳이 사라졌으므로 이 표는
--   기능적으로 죽은 표이고, 내용물은 14조 2항이 말하는 바로 그 "가공 데이터의 복제본"이다.
--   쓰지도 않을 남의 데이터를 우리 DB 에 보관하는 것 자체가 위반 상태의 유지다. 그래서
--   비우는 것이 아니라 **표째로 없앤다.**
--
-- ★ 남는 것은 **링크 한 줄**이다. 15조가 막는 것은 자동화 접근과 데이터 전용이지 링크가
--   아니고, 링크는 오히려 그쪽으로 트래픽을 보낸다. `!환산` 은 인코딩한 URL 한 줄만
--   답하고 **카카오 미리보기 카드가 곧 답장**이다. 다시 시도하지 말 것 —
--   자세한 근거는 `Claude/research-KAKAO-BOT.md` §2.8.1.
--
-- ───────────────────────────────────────────────────────────────────────────────
-- 드롭 범위
-- ───────────────────────────────────────────────────────────────────────────────
-- `drop table` 은 그 표에 달린 **인덱스·RLS 정책·CHECK 제약·GRANT 를 함께** 가져간다.
-- 따로 지울 것이 없다. 이 표를 참조하는 외래키는 처음부터 없었고(독립 캐시), 트리거·함수도
-- 만들지 않았으므로 `cascade` 는 쓰지 않는다 — 예상 못 한 객체가 딸려가면 그때는 실패해서
-- 알려주는 편이 낫다.
--
-- `if exists` 로 적어 **표가 이미 없어도 성공**한다. 원격에 적용됐는지 여부와 무관하게
-- 이 마이그레이션은 한 번에 통과해야 한다.

drop table if exists public.scouter_stat_cache;

-- CLAUDE.md §0.3 — 모든 마이그레이션의 마지막 줄. 표를 없애는 방향이라 새로 노출될 컬럼은
-- 없지만, 호출을 빼면 "이번 건은 예외" 라는 선례가 남는다. 예외는 없다.
select public.assert_no_public_sensitive_columns();
