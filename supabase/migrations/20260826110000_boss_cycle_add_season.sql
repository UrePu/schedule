-- ═══════════════════════════════════════════════════════════════════════════════
-- M_Schedule · 시즌 보스를 **별도 주기**로 — 1/2: enum 값만 추가
-- ═══════════════════════════════════════════════════════════════════════════════
--
-- 발주 지시(2026-08-26): *"시즌보스는 주간과 관련없어. 주간, 월간, 시즌보스 이렇게
-- 세가지로 나눠"* · *"하지만 시즌보스는 주간마다 초기화돼"*
--
-- ⚠️ **마이그레이션이 둘로 나뉜 이유**: `alter type ... add value` 로 추가한 enum 값은
--    **같은 트랜잭션 안에서 쓸 수 없다**(Postgres 제약). 마이그레이션은 트랜잭션으로
--    감싸이므로 값 추가와 사용을 한 파일에 두면 실패한다. 그래서 이 파일은 값만 더하고
--    실제 사용은 20260826110001 이 한다.
alter type public.boss_cycle add value if not exists 'season';
