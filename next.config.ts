import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /*
   * ═══════════════════════════════════════════════════════════════════════════
   * 클라이언트 라우터 캐시 — **탭을 오갈 때 서버를 다시 부르지 않는다**
   * ═══════════════════════════════════════════════════════════════════════════
   * 발주자 지시(2026-08-18): *"기본적으로 모든 탭에서 같은 캐싱된 값을 사용할건데
   * 이렇게 길게 걸릴일이 뭐가있어?"*
   *
   * Next 15 부터 `dynamic` 기본값이 **0초**다 — 즉 방금 보고 온 화면으로 되돌아가도
   * RSC 페이로드를 처음부터 다시 받는다. 이 앱의 네 화면은 전부 세션 의존이라 전부
   * dynamic 이고, 그래서 탭을 한 번 오갈 때마다 서버 왕복이 그대로 다시 났다.
   *
   * 30초로 두면 **왔다 갔다 하는 동안에는 서버 일이 0** 이 된다. 30초를 고른 이유는
   * 우리 DB 티어(`STALE_TIME.db` 60초)보다 짧기 때문이다 — 라우터 캐시가 쿼리 캐시보다
   * 오래 살면 "화면은 옛날 것, 데이터는 새것"이 어긋나 보일 수 있다.
   *
   * ⚠️ **낡은 데이터가 새 데이터를 덮어쓰지 않는다.** 라우터 캐시에서 재생된 RSC
   *    페이로드에는 서버가 만든 `dehydrate()` 스냅샷이 들어 있지만, TanStack Query 의
   *    하이드레이션은 `dataUpdatedAt` 이 **더 새로울 때만** 캐시를 갈아 끼운다
   *    (query-core `hydration.js`). 뮤테이션으로 갱신된 값이 되돌아가지 않는다.
   * ⚠️ `router.refresh()` 는 라우터 캐시를 비운다 — 로그인/로그아웃처럼 **화면 모양**이
   *    바뀌는 경로(§2.4 Rule 3)는 지금까지처럼 정확하게 동작한다.
   */
  experimental: {
    staleTimes: {
      dynamic: 30,
      static: 180,
    },
  },
};

export default nextConfig;
